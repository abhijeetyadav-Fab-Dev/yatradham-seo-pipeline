services:
  - type: web
    name: yatradham-seo-pipeline
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DRY_RUN
        value: "false"
      - key: OPENROUTER_API_KEY
        sync: false
